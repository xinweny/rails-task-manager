<%= form_for @task do |f| %>
    <div class =”form-group”>
        <%= f.label :title %>
        <%= f.text_field :title %>
    </div>

    <div class =”form-group”>
        <%= f.label :details %>
        <%= f.text_field :details %>
    </div>

    <% unless @task.new_record? %>
      <div class =”form-group”>
        <%= f.label :toggle_complete %>
        <%= f.check_box :completed, as: :boolean, checked: true %>
      </div>
    <% end %>

    <%= f.submit %>
<% end %>
